schema_version: 1
skill_key: recovery-plan-design
semantic_version: 1.0.0
name: 恢复计划设计
purpose: 将已验证问题转化为有负责人、依赖和截止条件的恢复方案。
allowed_roles: [delivery_planner, resolution_coordinator]
inputs: [validated_risks, constraints]
outputs: [recovery_options, action_items, approval_needs]
procedure: [定义恢复目标, 生成多种方案, 建立行动项与依赖, 识别对外承诺]
tools: []
permissions: {data_scope: current_mission, external_write: false}
quality_gates: [行动项有负责人和完成条件, 交期和预算变化进入审批]
