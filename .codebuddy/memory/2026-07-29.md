# 2026-07-29

## 推送 workbuddy_commercial_ga_v1_0 到 GitHub
- 仓库初始状态：无提交、无远程、无 .gitignore。
- 新增 `.gitignore`（排除 `*.db`、`*.egg-info/`、`.env`、`__pycache__`、`.terraform` 等）。
- 首提交 `845ef50 chore: initial commit (commercial GA v1.0)`，约 239 个文件；`workbuddy.db` 与 `egg-info` 已排除未入库。
- 提交身份未配置，暂用临时身份 `WorkBuddy Dev <dev@workbuddy.local>`（未改全局 git config）。
- HTTPS 在非交互环境无法认证 -> 改用 SSH。
- 远程：`git remote set-url origin git@github.com:qq547820639/workbuddy_commercial_ga_v1_0.git`；`git push -u origin main` 成功，分支 `main` 跟踪 `origin/main`。
