# 2026-07-31

## 仓库同步（workbuddy_commercial_ga_v1_0）
- 用户提供了 GitHub 仓库链接 https://github.com/qq547820639/workbuddy_commercial_ga_v1_0，即当前工作区对应的远程仓库。
- 操作：`git pull` 成功（fast-forward 845ef50 → 26a4dc1），拉取了远程新提交（commercial 服务拆分、团队路由、openapi.yaml 等）。
- 本地工作树此前被误删大量已跟踪文件；pull 自动恢复了大部分，剩余 9 个（Dockerfile / Makefile / RELEASE_NOTES.md / backlog 三个 csv / config 两个 yaml / evidence_catalog.yaml）经 `git cat-file` 确认在 HEAD 中存在，用 `git restore .` 全部恢复。
- 现状：本地 `main` 与 `origin/main` 一致（均 26a4dc1），工作树干净，仅 `.codebuddy/`（未跟踪，非仓库内容）。

## 全面技术评估（software-workbuddy-review 团队）
- 主理人齐活林建队 software-workbuddy-review，分派架构师高见远 + 产品经理许清楚，对 workbuddy_commercial_ga_v1_0 做纯分析评估（未改文件）。
- 架构师结论：设计纪律与诚实发布门（NO_GO）工程质量佳；主要风险在安全默认值 fail-open（auth_mode 默认 local_headers 全角色、默认弱密钥单密钥多用、billing webhook 单租户无鉴权、manual provider 无 secret 接受任意签名、SQLite 无 RLS）、循环依赖延迟导入、状态机定义散落、Outbox 仅 stdout、N+1 与审计串行锁。
- 产品经理结论：功能完整性 85/100，76/76 测试实跑通过；声明与实现高度对齐；短板为 Stripe 支付显式桩、配额只计量不拦截、前端控制台极薄(285行)、文档数字漂移(133→146路径/0019→0020迁移)、上线清单自报布尔未与 pilot 门禁交叉校验。
- 最高优先修复：S4+S5 —— 默认配置下未鉴权 manual webhook 可直接置发票 PAID，击穿诚实边界。
- 待用户决策：是否落盘交付报告 / 是否启动 P0 安全补丁（标准 SOP 或 BugFix 路径）。

## 代码重构审查（五类问题，未改文件）
- 用户要求对 workbuddy_commercial_ga_v1_0 做全面重构分析（结构/命名/重复/职责/耦合），交付结构化报告而非改码。
- 落盘报告：docs/refactor-review-2026-07-31.md。核心发现：
  - 结构：api/main.py 的 create_app() 是 God Function（602行/60路由/37 commit/19 require_tenant），Gmail 同步逻辑堆在路由里。
  - 重复：main.py L385 与 L432 的 21 模型删除列表逐字复制；gmail_sync 与 gmail_webhook 的 upsert 循环重复；~40 路由重复依赖注入样板。
  - 命名：services/billing/（支付适配器）与 services/commercial/billing.py（计费领域）同名撞车；business.py(689) 泛化命名实为四条生命周期总控。
  - 职责：connectors/base.py 定义 MailConnector 协议但 Gmail/MicrosoftGraph 连接器均未遵守；gate_signing._signing_key() 有注释承诺却空 pass 的死分支。
  - 耦合：37 处 session.commit() 在路由层，事务边界与 HTTP 耦合，建议引入 unit_of_work 依赖。
- 重构路线按 P0（提取/去死代码，零风险）→ P1（抽象边界+事务，测试护航）→ P2（结构美化）推进；强调用 pytest + generate_openapi diff 保证功能不变。
- 备注：build/ 是 src/ 的完整副本（已被 .gitignore 忽略），属构建产物卫生项，非源码问题。
